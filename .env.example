# Database connection for local development
DATABASE_URL="prisma+postgres://..."
DIRECT_URL="postgres://..."

# Authentication secret for session token signing
AUTH_SECRET="replace-with-a-secure-secret"
